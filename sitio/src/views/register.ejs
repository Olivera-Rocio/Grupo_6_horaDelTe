<!DOCTYPE html>
<html lang="es">

<%- include("partials/head",{title: 'Registro de usuario'}) %>

<body class="register">

    <%- include("partials/header") %>

    <main>
        <div class="formulario">
            <p>registrate</p>
            <form action="" method="POST">
                <div class="box-form">
                    <label class="nombreCompleto" for="nombre">Nombre</label>
                    <input type="text" id="nombre" name="name">
                    <span class= "error"><%= locals.errores && errores.name ? errores.name.msg : null %></span>
                </div>
                <div class="box-form">
                    <label class="email" for="email">Email</label>
                    <input type="email" id="email" name="email">
                    <span class= "error"><%= locals.errores && errores.email ? errores.email.msg : null %></span>
                </div>
                <div class="box-form">
                    <label class="phone" for="phone">Teléfono</label>
                    <input type="tel" id="phone" name="phone">
                    <span class= "error"><%= locals.errores && errores.phone ? errores.phone.msg : null %></span>
                </div>
                <div class="box-form">
                    <label for="pass">Contraseña</label>
                    <input type="password" id="pass" name="password">
                    <span class= "error"><%= locals.errores && errores.password ? errores.password.msg : null %></span>
                </div>
                <div class="box-form">
                    <label for="password">Confirmar contraseña</label>
                    <input type="password" id="password2" name="password2">
                    <span class= "error"><%= locals.errores && errores.password2 ? errores.password2.msg : null %></span>
                </div>
                <div class="registrarse">
                    <button type="submit">registrarse</button>
                </div>
            </form>
        </div>
        <div class="iniciar-sesion">
            <p>¿Tenes cuenta?<a href="/users/login"> Haz click para iniciar sesion</a></p>
        </div>
    </main>
    
    <%- include("partials/footer") %>
    
</body>

</html>